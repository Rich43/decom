<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android" xmlns:app="http://schemas.android.com/apk/res-auto"
    android:background="#000000"
    android:layout_width="match_parent"
    android:layout_height="match_parent">
    <SurfaceView
        android:id="@+id/surfaceView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"/>
    <com.king.zxing.ViewfinderView
        android:id="@+id/viewfinderView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:cornerColor="#009688"
        app:frameColor="#ffffff"
        app:gridHeight="0dp"
        app:labelText="Scan the QR-code to connect\10 Please ensure your devices are connected to the some network"
        app:labelTextColor="#ffffff"
        app:labelTextLocation="bottom"
        app:labelTextSize="@dimen/size_12sp"
        app:laserColor="#009688"
        app:laserStyle="line"
        app:maskColor="#000000"/>
    <TextView
        android:textColor="?android:attr/textColorPrimary"
        android:id="@+id/ivFlash"
        android:visibility="gone"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="560dp"
        android:text="@string/qr_flashlight"
        android:drawableTop="@drawable/ic_qr_flash_selector"
        android:layout_centerHorizontal="true"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        style="@style/OnClick"/>
    <include
        android:id="@+id/vg_header"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        layout="@layout/luma_header"/>
    <ImageView
        android:id="@+id/iv_qrcode_back"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="@dimen/size_20dp"
        android:src="0x7f070224"
        android:layout_below="@+id/vg_header"/>
    <TextView
        android:textSize="@dimen/size_16dp"
        android:textColor="#ffffff"
        android:gravity="center"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/size_50dp"
        android:text="Scan"
        android:layout_below="@+id/iv_qrcode_back"/>
</RelativeLayout>
